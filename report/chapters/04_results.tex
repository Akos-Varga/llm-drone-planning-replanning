\chapter{Results}

This chapter presents the evaluation results of the proposed LLM-based drone planning and replanning
architecture. The evaluation is divided into three parts. First, the individual LLM-based components are
evaluated. 
Second, the selected components are integrated and evaluated in an end-to-end simulation case study. 
Finally, the system is tested on the physical drone platform to assess its behaviour under real-world execution
conditions.

\section{Component-Level LLM Evaluation}

This section evaluates the two LLM-based components used in the proposed architecture:
the cloud-based planner and the task admission module. The purpose of this
evaluation is to select the models used in the subsequent simulation and physical experiments.

\subsection{Cloud-based Planning Pipeline}

The cloud-based planner was evaluated to determine which model provided the best trade-off between
schedule quality, inference time, and reliability. Both cloud-based and locally deployable models were
initially considered. However, due to hardware constraints and real-time execution requirements, small
quantized LLMs with 1--3 billion parameters were found to be too slow and unreliable for planning,
even for tasks consisting of only one or two subtasks. The final cloud-based evaluation therefore used
GPT-5, GPT-5-mini, GPT-4o, and GPT-4.1 through the OpenAI API \cite{openai_models}.

\subsubsection{Evaluation Setup}
The models were tested on tasks containing between one and ten subtasks. For each subtask count,
three task descriptions were evaluated. The task descriptions were written in unstructured natural
language with varied wording and sentence structure in order to test the models' ability to interpret
high-level mission instructions.

The generated schedules were compared against an optimal schedule computed using a VRP solver
\cite{google_vrp}. The VRP solution was used as a baseline for evaluating the makespan of the
LLM-generated mission schedules. A generated schedule was considered successful if it produced a valid
sequence of drone actions satisfying all subtasks.

\subsubsection{Schedule Quality}
\Cref{tab:avg_vrp_gap_by_subtasks} shows the average gap between the makespan generated by each model
and the VRP baseline. The table also reports the number of successfully generated schedules out of
three for each subtask count.

\input{tables/cloud_avg_vrp_gap_by_subtasks.tex}

\input{tables/cloud_avg_inference_time_by_subtasks.tex}

\subsubsection{Inference Time}
\Cref{tab:avg_inference_time_by_subtasks} shows the average inference time for each model, computed
only over successfully generated schedules.

\subsubsection{Model Selection}
The results show that GPT-5 consistently generated schedules close to the VRP baseline, but required
substantially longer inference times than GPT-5-mini. GPT-5-mini produced valid schedules for most
tasks and achieved near-optimal schedules for tasks with up to five subtasks, while requiring
considerably less inference time. GPT-4o and GPT-4.1 performed well on simpler tasks, but increasingly
produced suboptimal or invalid schedules as the number of subtasks increased.

Based on this trade-off between schedule quality, inference time, and API cost, GPT-5-mini was selected
for the cloud-based planning pipeline.

\subsection{Local Task Admission Module}
The task admission module has different requirements from the cloud-based planner. While the
planner must produce high-quality mission schedules, the admission module must evaluate drone telemetry
quickly and reliably during execution. Since this module directly affects whether a task is accepted,
rejected, or sent for replanning, reliability is particularly important.

\subsubsection{Evaluation Setup}
The evaluated models were tested on four categories: acceptable tasks, drone-state errors,
link-quality errors, and flight-time errors. Each category contained 20 test cases, resulting in 80
test cases per model. Seven 4-bit quantized models, ranging from 0.6 to 4 billion parameters, were
evaluated from the Qwen \cite{ollama_qwen25,ollama_qwen3}, Gemma \cite{ollama_gemma3},
Llama \cite{ollama_llama32}, and Phi \cite{ollama_phi4} model families.

\subsubsection{Classification Accuracy}
\Cref{tab:task_admission_accuracy} reports the number of correctly classified cases out of 20 for each category. 

\input{tables/task_admission_accuracy.tex}

\subsubsection{Inference Time}
\Cref{tab:task_admission_inference_time} reports the average and maximum inference time for each model.
\input{tables/task_admission_inference_time.tex}

\subsubsection{Model Selection}
The results show that \texttt{qwen3:1.7b} achieved perfect classification accuracy across all four categories,
correctly solving all 80 test cases. Although some models achieved lower average inference times, they
failed on safety-relevant categories such as link-quality or flight-time errors. Since the task
admission module directly affects replanning decisions, classification reliability was prioritized over
minimal inference time. Therefore, \texttt{qwen3:1.7b} was selected for the local task admission module.

\section{Simulation Results}

This section evaluates the planning and task admission components in a simulated
multi-drone mission environment. The purpose of the simulation is to validate the interaction
between cloud-based planning, locall task admission, execution monitoring, failure detection,
and replanning before deployement on a physical drone platform.

\subsection{Simulation Setup}
The simulation environment was designed to represent a multi-drone inspection scenario with static
objects of interest distributed across a two-dimensional area.

The simulated drone team consists of multiple drones with predefined capabilities. Depending on the
assigned subtask, a drone may be required to capture RGB images, record video, acquire thermal images,
inspect structures, or measure wind levels. The simulator abstracts away low-level flight dynamics and
instead focuses on task-level execution, including task allocation, admission checking, failure
handling, and replanning.

To evaluate the system response to execution-time changes, the simulator also models battery drainage,
link-quality variations, and drone failures. Battery levels decrease during mission execution, while
link quality may change over time and affect whether a drone can safely accept a task. Drone failures
can occur during execution, causing the affected drone's remaining subtasks to be returned to the
cloud-based planner for rescheduling. These simulated disturbances allow the replanning and task
admission mechanisms to be evaluated under dynamic mission conditions.

\subsection{Simulation Case Study}
A representative mission execution was conducted in the simulation environment using the selected
language models for planning and task admission. The drone team executed the following mission:

\begin{quote}
Document the condition of all houses with video and inspect each rooftop, while measuring wind levels
near the Base and Tower. In addition, take an RGB image of the Tower.
\end{quote}

Based on the submitted natural-language mission, the cloud-based planner generated a schedule,
and the resulting subtasks were sent to the drones for admission checking.

\Cref{tab:sim_subtasks_suitable_drones} shows the decomposed subtasks and the drones from the world
configuration that have the required skill to execute each subtask.

\input{tables/simulation_setup.tex}

\Cref{fig:sim_execution_frames_1,fig:sim_execution_frames_2,fig:sim_execution_frames_3} show the
main stages of the execution:

\begin{enumerate}
    \item All drones accept their assigned subtasks and start moving toward their targets
    (\mbox{\Cref{fig:frame_1}}).
    \item Once the drones arrive at their assigned targets, they begin executing their subtasks
    (\Cref{fig:frame_2}).
    \item Drone5's local task admission module signals failure during the execution phase of SubTask4.
    The affected remaining subtasks are sent back to the cloud-based planner for rescheduling (\Cref{fig:frame_3}).
    \item While the updated schedule is being generated, the other drones continue executing their
    previously allocated subtasks (\Cref{fig:frame_4}).
    \item SubTask4 is reassigned to Drone6 and acknowledged by the local task admission module
    (\Cref{fig:frame_5}).
    \item All subtasks are completed by the drone team (\Cref{fig:frame_6}).
\end{enumerate}

\Cref{tab:simulation_case_summary} summarizes the system response during the simulated replanning case study.

\input{tables/simulation_results.tex}

This qualitative case study demonstrates that the implemented architecture can coordinate the
cloud-based planner, local task admission module, and execution monitor within a single mission
workflow. In particular, the sequence shows that the system can detect a drone failure, update the
mission state, reschedule affected subtasks, and continue execution with the remaining available drones.

\begin{landscape}
\begin{figure}[p]
    \centering

    \begin{subfigure}{0.49\linewidth}
        \centering
        \simframe{figures/frames/1.png}
        \caption{First allocation round.}
        \label{fig:frame_1}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.49\linewidth}
        \centering
        \simframe{figures/frames/2.png}
        \caption{The arrived drones start executing their tasks.}
        \label{fig:frame_2}
    \end{subfigure}

    \caption{Initial stages of the simulated mission execution.}
    \label{fig:sim_execution_frames_1}
\end{figure}
\end{landscape}

\begin{landscape}
\begin{figure}[p]
    \centering

    \begin{subfigure}{0.49\linewidth}
        \centering
        \simframe{figures/frames/3.png}
        \caption{Drone5's local task admission module signals failure during task execution.}
        \label{fig:frame_3}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.49\linewidth}
        \centering
        \simframe{figures/frames/4.png}
        \caption{The remaining drones continue executing their originally allocated subtasks while the cloud-based planner reschedules the failed task.}
        \label{fig:frame_4}
    \end{subfigure}

    \caption{Intermediate stages of the simulated mission execution.}
    \label{fig:sim_execution_frames_2}
\end{figure}
\end{landscape}

\begin{landscape}
\begin{figure}[p]
    \centering

    \begin{subfigure}{0.49\linewidth}
        \centering
        \simframe{figures/frames/5.png}
        \caption{SubTask4 is allocated and acknowledged by Drone6.}
        \label{fig:frame_5}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.49\linewidth}
        \centering
        \simframe{figures/frames/6.png}
        \caption{All subtasks are completed.}
        \label{fig:frame_6}
    \end{subfigure}

    \caption{Final stages of the simulated mission execution.}
    \label{fig:sim_execution_frames_3}
\end{figure}
\end{landscape}

% \section{Experimental Results}
% This section presents the physical experiments conducted on the drone platform. Unlike the simulation
% case study, the physical experiments did not evaluate the complete planning, onboard admission,
% failure detection, and replanning pipeline. Due to time and integration constraints, the real-drone
% experiments were limited to validating the main mission execution pipeline. 
% The purpose of these experiments is therefore to demonstrate that the core drone
% control and task execution components can operate on the physical platform, while the full
% LLM-based replanning architecture is evaluated in simulation.

\section{Physical Platform and Experimental Setup}

This section describes the physical drone platform, laboratory environment, and control interface used
as the basis for real-world deployment of the proposed architecture.

\subsection{Scope of Physical Integration}

The full closed-loop architecture, including cloud-based planning, local LLM-based task admission,
failure detection, and replanning, was not deployed on the physical drones within the scope of this
thesis. Instead, the physical platform is described to show the intended deployment environment and the
connection between the symbolic drone skills used by the planner and the capabilities of the Anafi
drones.

The complete system behavior is therefore evaluated in the simulation results, while the physical
platform description provides context for future real-world deployment.

\subsection{Drone Platform}
The physical platform was based on drones from the Parrot Anafi family. To represent a
drone team with heterogeneous sensing capabilities, four variants were considered: the
Parrot Anafi 4K, Anafi Thermal, Anafi USA, and Anafi Ai. These platforms are shown in
\Cref{fig:drone_platforms}.

\begin{figure}[H]
    \centering

    \begin{subfigure}{0.25\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/drones/4k.png}
        \caption{Anafi 4K.}
        \label{fig:drone_anafi_4k}
    \end{subfigure}
    \hspace{0.02\textwidth}
    \begin{subfigure}{0.25\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/drones/thermal.png}
        \caption{Anafi Thermal.}
        \label{fig:drone_anafi_thermal}
    \end{subfigure}

    \vspace{0.3em}

    \begin{subfigure}{0.25\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/drones/usa.png}
        \caption{Anafi USA.}
        \label{fig:drone_anafi_usa}
    \end{subfigure}
    \hspace{0.02\textwidth}
    \begin{subfigure}{0.25\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/drones/ai.png}
        \caption{Anafi Ai.}
        \label{fig:drone_anafi_ai}
    \end{subfigure}

    \caption[Drone platforms used during physical testing.]%
            {Drone platforms used during physical testing.
            Images adapted from Parrot product documentation
            \cite{parrot_4k,parrot_thermal,parrot_usa,parrot_ai}.}
    \label{fig:drone_platforms}
\end{figure}

\Cref{tab:anafi_capabilities} summarizes the project-relevant specifications of the
drone variants used in the experiments, together with the symbolic skills assigned to
each platform in the planner.

\input{tables/anafi_drones.tex}

\subsection{Laboratory and Control Setup}

The intended physical setup consists of an indoor laboratory environment, a Vicon-based localization
system, and a ROS-based control interface for communicating with the Anafi drones. This setup provides
the infrastructure required for executing high-level mission commands on the physical drone platform.

\subsubsection{Laboratory Environment}

The laboratory environment is equipped with a Vicon motion capture system \cite{vicon_tracker}.
Since GPS-based localization is not available indoors, the Vicon system can provide position and
orientation feedback during flight. Reflective markers can be mounted on the drone, allowing the
motion capture system to track the drone within the available flight area.

Similar target objects to those used in the simulation environment can be defined in the physical
setup. Unlike the two-dimensional simulation setup, the laboratory environment is represented in three
dimensions. Each target object can be assigned a position in the Vicon coordinate frame, allowing the
drone to navigate to target locations using 3D position feedback.

\subsubsection{Drone Control Interface}

The physical setup uses the \texttt{anafi\_autonomy} ROS package
\cite{sarabakha2023anafiros} as the control interface between the mission execution pipeline and the
Parrot Anafi drones. The package provides ROS topics and services for accessing sensor data,
controlling the drones through high-level mission commands such as takeoff, landing, and navigation,
and obtaining the drone-state information required by the task admission module, such as battery level,
flight state, position feedback, and communication status.

The LLM-based planner does not directly control the drone hardware. Instead, it generates symbolic
subtasks, which are translated by the mission execution layer into commands supported by the drone
control interface. This separation allows the planning system to remain platform-independent, while
the execution layer handles platform-specific communication with the Anafi drones.

% \subsection{Experimental Setup}
% This setup validates the execution layer of the proposed architecture. While the full LLM-based
% replanning loop was not deployed on the physical drones, the experiments confirmed that generated
% mission actions could be transferred to and executed by the Anafi control interface.

% \subsubsection{Laboratory Environment}
% The physical experiments were conducted in an indoor laboratory environment equipped with a Vicon
% motion capture system \cite{vicon_tracker}. The Vicon system was used to provide accurate position feedback for the drone
% during flight, since GPS-based localization is not available indoors. Reflective markers were mounted
% on the drone, allowing the motion capture system to track its position and orientation within the
% flight area.

% Similar target objects to those used in the simulation environment were defined for the physical
% experiments. However, unlike the two-dimensional simulation setup, the experimental environment was
% represented in three dimensions. Each target object was assigned a position in the Vicon coordinate
% frame, allowing the drone to navigate to target locations using 3D position feedback.

% \subsubsection{Drone Control Interface}
% The physical drone experiments used the \texttt{anafi\_autonomy} ROS package \cite{sarabakha2023anafiros} as the control interface
% between the mission execution pipeline and the Parrot Anafi drones. The package provides ROS topics,
% services for controlling the drone and accessing sensor data. In this thesis, it was used
% to execute high-level mission commands such as takeoff, landing, navigation.

% The LLM-based planner does not directly control the drone hardware. Instead, it generates symbolic
% subtasks, which are translated by the mission execution layer into commands supported by the drone
% control interface. This separation allows the planning system to remain platform-independent, while
% the execution layer handles the platform-specific communication with the Anafi drones.